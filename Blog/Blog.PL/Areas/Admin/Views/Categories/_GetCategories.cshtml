@model IEnumerable<Blog.PL.Areas.Admin.ViewModels.CategoryViewModel>;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@foreach(var user in Model)
{
    <div class="card">
        <div class="mt-3">@user.Name</div>
    </div>
}